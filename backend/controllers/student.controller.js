const User = require('../models/User');
const Course = require('../models/Course');

const getStudentDashboard = async (req, res) => {
  // Student dashboard logic
};

const getMyModules = async (req, res) => {
  // Student modules logic
};

module.exports = {
  getStudentDashboard,
  getMyModules
};